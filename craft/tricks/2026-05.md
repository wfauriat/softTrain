# Tricks — May 2026

A dated journal of small wins. Each entry: 1-3 lines on the trick, why it matters, where you encountered it.

Append new entries via `tools/new-trick.sh "<the trick>"` — keeps the format consistent.

---

## 2026-05-06

- `tools/from-scaffold.sh python-cli-typer ~/code/foo` is faster than `cookiecutter` because we wrote the scaffolds. Why it matters: a 1-second copy-and-rename beats fishing through cookiecutter prompts. Encountered: while building this repo.
